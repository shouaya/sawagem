import React, { Component } from 'react';
import MiniPage from '../tools/Page';
class {{page_name}} extends Component {
  
  render() {
    const column = [
      {{# rows_model}}
        {{^jsonignore}}
          {"title":"{{title}}", "name":"{{name}}", "type":"{{type_js}}", "ref":"{{join_column}}", "null":{{nullable}}, "unique":{{unique}} ,"key": {{is_searchkey}} }, 
        {{/jsonignore}}
      {{/ rows_model}}
    ]
    
    return (
      <MiniPage
        title="{{model_title}}"
        name="{{resource_path}}" 
        table_name="{{table_name}}" 
        column={column}
      />
    );
  }
}

export default {{page_name}};
